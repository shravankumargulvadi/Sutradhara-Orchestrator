# Host-side bind mount source used by the `dev` service.
HOST_ROS2_WS_AI_ROOT=.

# Linux desktop only. Leave blank for headless use.
DISPLAY=
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=all
